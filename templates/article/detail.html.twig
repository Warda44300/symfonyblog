{% extends 'base.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}
<div class="row mb-5">
	<div class="col-12 col-lg-6 mt-5">
		<img src="{{ article.image }}" height="150px" width="auto"/>
	</div>
	<div class="col-12 col-lg-6 mt-5">
	    <h1>{{ article.title }}</h1>
	    <p>{{ article.description }}</p>
	</div>
</div>

{% endblock %}
